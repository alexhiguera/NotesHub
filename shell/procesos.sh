# Procesos

# Muestra los procesos que se están ejecutando en el sistema
ps
# Muestra los procesos que se están ejecutando en el sistema con más información
ps -ef
ps -aux
# Formato árbol
ps lf
# Muestra los procesos que se están ejecutando en el sistema con más información y en formato árbol
pstree

# Listados de shokets (puertos)
lsof
lsof -i
lsof -i :80

# Visualizador de procesos
top
htop
